# What does each method return in the following example?

arr = [15, 7, 18, 5, 12, 8, 5, 1]

1. arr.index(5)
  # index 3 is returned
2. arr.index[5]
  # returns error because of incorrect brackets. can't provide index with .index method
3. arr[5]
  # returns 8 